
# 🚀 دليل تشغيل ونشر "إليكترو بريميوم" على GitHub

لتحويل هذه الملفات إلى موقع يعمل برابط (مثال: `username.github.io/repo`)، اتبع الخطوات التالية:

### الخطوة 1: إنشاء مستودع (Repository) جديد
1. ادخل على حسابك في [GitHub](https://github.com).
2. اضغط على زر **New** لإنشاء مستودع جديد.
3. سمِّ المستودع أي اسم (مثلاً: `electro-premium`).
4. اجعله **Public** ثم اضغط **Create repository**.

### الخطوة 2: رفع الملفات
- يمكنك استخدام **GitHub Desktop** أو رفع الملفات يدوياً بالضغط على **"uploading an existing file"** في صفحة المستودع.
- **تنبيه:** ارفع كافة الملفات والمجلدات (بما فيها `package.json` ومجلد `components`).

### الخطوة 3: تفعيل إعدادات النشر (GitHub Pages)
1. من أعلى الصفحة في GitHub، اضغط على **Settings**.
2. من القائمة الجانبية اليسرى، اضغط على **Pages**.
3. تحت قسم **Build and deployment**، وفي خانة **Source**، اختر: 
   `GitHub Actions` بدلاً من `Deploy from a branch`.

### الخطوة 4: إضافة مفتاح الذكاء الاصطناعي (API KEY)
لكي يعمل وصف المنتجات التلقائي، يجب إضافة المفتاح:
1. في **Settings**، اذهب إلى **Secrets and variables** -> **Actions**.
2. اضغط على **New repository secret**.
3. الاسم: `API_KEY`
4. القيمة: ضع مفتاح Gemini الخاص بك.

### الخطوة 5: النشر
بمجرد رفع الملفات وتغيير المصدر إلى GitHub Actions، سيقوم المشروع بالبناء تلقائياً. يمكنك رؤية التقدم في تبويب **Actions**. عند الانتهاء، سيظهر لك الرابط في صفحة **Settings -> Pages**.

---
💡 **نصيحة للمحترفين:** إذا كنت تريد ربط دومين `.com` خاص بك، يمكنك القيام بذلك من صفحة **Settings -> Pages** في خانة **Custom domain**.
